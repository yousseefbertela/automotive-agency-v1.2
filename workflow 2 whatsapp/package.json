{
  "name": "wa-response-webhook",
  "version": "1.0.0",
  "description": "WhatsApp Business webhook — quotation confirmation/cancellation handler (from n8n workflow)",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "jest --forceExit --detectOpenHandles"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "firebase-admin": "^13.0.2",
    "uuid": "^11.0.5"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testMatch": ["**/tests/**/*.test.js"]
  }
}
